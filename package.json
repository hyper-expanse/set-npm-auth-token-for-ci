{
  "author": {
    "name": "Hutson Betts",
    "email": "hutson@hyper-expanse.net",
    "url": "https://gitlab.com/hutson"
  },
  "bin": "",
  "bugs": "https://gitlab.com/hyper-expanse/set-npm-auth-token-for-ci/issues",
  "description": "Set authentication token placeholder into `.npmrc` file for use in automated CI processes.",
  "engines": {
    "node": ">=4.2.0",
    "npm": ">=2.1.9"
  },
  "files": [
    "src/"
  ],
  "homepage": "https://gitlab.com/hyper-expanse/set-npm-auth-token-for-ci#readme",
  "keywords": [
    "automation",
    "npmrc",
    "npm",
    "ci",
    "npm_token"
  ],
  "license": "Apache-2.0",
  "main": "src/index.js",
  "name": "set-npm-auth-token-for-ci",
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/hyper-expanse/set-npm-auth-token-for-ci.git"
  },
  "scripts": {
    "dev": "docker run --rm --user node -v \"$(pwd)\":/app -w /app -it node:4 sh -c \"yarn install; yarn test; bash\"",
    "test": "eslint src/ && nyc --all --cache --include=src/ --exclude=src/cli.js --exclude=src/**/*.spec.js --exclude=src/**/*.mocks.js --reporter=lcov --reporter=text mocha --check-leaks --full-trace --globals __core-js_shared__,__coverage__,YamlEscaper --inline-diffs --no-exit --recursive --reporter=progress --retries 1 src/**/*.spec.js"
  },
  "version": "1.0.0",
  "dependencies": {
    "debug": "^3.0.0",
    "local-or-home-npmrc": "^1.1.0",
    "registry-url": "^3.1.0"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "codecov": "^3.0.0",
    "eslint": "^4.0.0",
    "eslint-config-xo-space": "^0.18.0",
    "mocha": "^5.0.0",
    "nock": "^9.0.0",
    "npm-publish-git-tag": "^1.0.5",
    "nyc": "^11.0.2",
    "semantic-release-gitlab": "^5.0.0",
    "sinon": "^5.0.0",
    "sinon-chai": "^2.12.0",
    "tmp": "^0.0.33"
  }
}
